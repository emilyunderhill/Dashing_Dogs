<% @outfits.each do |outfit| %>
  <% if user_signed_in? %>
    <% next if current_user == outfit.user %>
    <div class="TO DO: OUTFIT CARD">
      <% outfit.photos.each do |photo|%>
        <div>
          <%= cl_image_tag photo.key, crop: :fill, width: 200, height: 200 %>
        </div>
      <%end%>
      <div class="d-flex">
        <%= outfit.name.capitalize %>
        <%= outfit.size.capitalize %>
        £<%= outfit.price %> / day
      </div>
      <div class="d-flex">
        <%= link_to "View", outfit_path(outfit), class: "btn btn-primary" %>
        <%= link_to "Book!", new_outfit_booking_path(outfit), class: "btn btn-success" %>
      </div>
    </div>
  <% else %>
    <div class="TO DO: OUTFIT CARD">
        <% outfit.photos.each do |photo|%>
          <div>
            <%= cl_image_tag photo.key, crop: :fill, width: 200, height: 200 %>
          </div>
        <%end%>
        <div class="d-flex">
          <%= outfit.name.capitalize %>
          <%= outfit.size.capitalize %>
          £<%= outfit.price %> / day
        </div>
        <div class="d-flex">
          <%= link_to "View", outfit_path(outfit), class: "btn btn-primary" %>
          <%= link_to "Book!", new_outfit_booking_path(outfit), class: "btn btn-success" %>
        </div>
      </div>
  <% end %>

<% end %>
